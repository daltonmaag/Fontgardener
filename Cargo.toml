[package]
name = "fontgardener"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
csv = "1.1"
norad = { path = "../norad", features = ["rayon"] }
plist = { version = "1.3.1", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
